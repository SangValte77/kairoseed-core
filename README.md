# kairoseed-core
Minimal reproducible kernel for observable attractor dynamics and σ* transition experiments.

## Quick Start

Install dependencies:

```bash
pip install -r requirements.txt
```
# Kairoseed Core

Minimal reproducible stochastic dynamics framework
for observable coherence-to-diffusion transitions.

![Phase Transition Diagram](outputs/phase_transition_diagram.png)

## Observable Phenomena

- trajectory convergence
- entropy increase under stochastic forcing
- coherence breakdown near transition regimes
- ensemble-consistent phase behavior

## Quick Start

pip install -r requirements.txt
python experiments/plot_phase_transition.py
